using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Color { RED, BLUE, YELLOW, GREEN };


public enum TileType { NORMAL, END, SAFE};

public enum UnitState { HOME, ONBOARD, ONEND, FINISH}; // If the unit is inside the home or on the board or finished the game.

public enum TileColor { BLUE, GREEN, YELLOW, RED, WHITE};

